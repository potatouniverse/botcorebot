import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable experimental features for edge runtime if needed
  experimental: {
    // Useful for serverless functions
  },
};

export default nextConfig;
